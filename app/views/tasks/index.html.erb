<h1><strong>My tasks</strong></h1>

<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task.id) %> Completed: <%= task.completed %>
  <% end %>
</ul>

<%= link_to "Add a new task", new_task_path %>
